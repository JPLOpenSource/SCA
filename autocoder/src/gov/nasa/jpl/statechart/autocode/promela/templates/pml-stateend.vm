##
## Done with state!
##
##- input vars (in addition to $model, etc.):
##    * $stateName:  name of current State Machine state being implemented
###############################################################################
#*--*##set( $s = $mapper.indentation() )
#*--*##if( !$skipElseBranch )
${s}:: else
#*--*##end
${s}fi;
#*--*##set( $discard = $mapper.decIndent() )## decrease indentation
#*--*##set( $s = $mapper.indentation() )
${s}od;
#*--*##set( $discard = $mapper.decIndent() )## decrease indentation
